
spring:
  cloud:
    loadbalancer:
      nacos:
        enabled: true
    nacos:
      serverAddr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848
  config:
    import:
      - nacos:qihang-oms.yaml?refresh=true
  application:
    name: oms-api

server:
  port: 8082